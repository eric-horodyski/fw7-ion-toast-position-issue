import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fw7-ion-toast-position-issue',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
